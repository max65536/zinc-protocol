/// <reference types="node" />
interface Guardian {
    index: number;
    key: string;
}
export declare class MockGuardians {
    setIndex: number;
    signers: Guardian[];
    constructor(setIndex: number, keys: string[]);
    getPublicKeys(): Buffer[];
    updateGuardianSetIndex(setIndex: number): void;
    addSignatures(message: Buffer, guardianIndices: number[]): Buffer;
}
export declare class MockEmitter {
    chain: number;
    address: Buffer;
    sequence: number;
    constructor(emitterAddress: string, chain: number, startSequence?: number);
    publishMessage(nonce: number, payload: Buffer, consistencyLevel: number, timestamp?: number, uptickSequence?: boolean): Buffer;
}
export declare class MockEthereumEmitter extends MockEmitter {
    constructor(emitterAddress: string, chain?: number);
}
export {};
