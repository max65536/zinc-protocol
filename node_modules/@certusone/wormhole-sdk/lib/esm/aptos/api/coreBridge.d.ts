import { Types } from "aptos";
import { ChainId } from "../../utils";
export declare const upgradeGuardianSet: (coreBridgeAddress: string, vaa: Uint8Array) => Types.EntryFunctionPayload;
export declare const initWormhole: (coreBridgeAddress: string, chainId: ChainId, governanceChainId: number, governanceContract: Uint8Array, initialGuardian: Uint8Array) => Types.EntryFunctionPayload;
